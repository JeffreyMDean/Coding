// Write a method that takes in two numbers and returns the first number subtracted by the second. Then run the method and print the result.

function subtractor(number1, number2) {
  return number1 - number2;
}

const inputNumber1 = 2;
const inputNumber2 = 5;
const result = subtractor(inputNumber1, inputNumber2);
console.log(result);
